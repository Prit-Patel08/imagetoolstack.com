---
import Root404Page from "../404.astro";

export async function getStaticPaths() {
  const locales = ['es', 'fr', 'de', 'pt', 'id'];
  return locales.map((locale) => ({
    params: { lang: locale },
  }));
}
---
<Root404Page />
