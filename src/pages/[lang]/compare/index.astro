---
import RootCompareIndexPage from "../../compare/index.astro";

export async function getStaticPaths() {
  const locales = ['es', 'fr', 'de', 'pt', 'id'];
  return locales.map((locale) => ({
    params: { lang: locale },
  }));
}
---
<RootCompareIndexPage />
